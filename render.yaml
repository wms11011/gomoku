# Render 一键部署配置（Blueprint）
# 在 Render 控制台选择 New → Blueprint，连接本仓库即可自动识别
services:
  - type: web
    name: gomoku
    runtime: node
    plan: free
    buildCommand: npm install
    startCommand: node server/server.js
    healthCheckPath: /
